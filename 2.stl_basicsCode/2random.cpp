#include <bits/stdc++.h>
using namespace std;

int main() {
    ios_base::sync_with_stdio(0);
    cin.tie(0);
    cout.tie(0);
    srand(time(0));
    int r=rand();
    cout<<r;
    //just by introducing seed with change in time seed changes and produces diff random no. 
    //where as when u use rand alone everytime it produces the same random no.
    return 0;
}
