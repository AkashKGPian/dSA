#include <bits/stdc++.h>
using namespace std;

int main() {
    ios_base::sync_with_stdio(0);
    cin.tie(0);
    cout.tie(0);
    vector<int> vec = {1,2,5,5,6,3,5,7};
    //iteratiing thorugh the vector using iterator
    // vector<int> :: iterator it;//scope resolution operator
    // for(it=vec.begin();it!=vec.end();it++){
    //     cout<<(*it)<<" ";
    // }
    return 0;
}
