#include <bits/stdc++.h>
using namespace std;

int main() {
    ios_base::sync_with_stdio(0);
    cin.tie(0);
    cout.tie(0);
    vector<int> v(5,2);
    for(int value:v){
        cout<<value<<" ";
    }

    return 0;
}
