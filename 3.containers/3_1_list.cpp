#include <bits/stdc++.h>
using namespace std;

int main() {
    ios_base::sync_with_stdio(0);
    cin.tie(0);
    cout.tie(0);
    list<int> ls;
    int temp;
    for(int i=0;i<5;i++){
        cin>>temp;
        ls.push_back(temp);
    }
    return 0;
}
