#include <bits/stdc++.h>
using namespace std;
//Nephren gives a riddle - cf

void strfill(string st, int level){
    if(level==0){
        st+="What are you doing at the end of the world? Are you busy? Will you save us?";
        return ;
    }
}

int main() {
    ios_base::sync_with_stdio(0);
    cin.tie(0);
    cout.tie(0);
    int q;
    cin>>q;
    while(q--){
        int n,k;
        cin>>n>>k;
        string st;
        strfill(st,n);
    }
    
    return 0;
}
